

<div class="row">
  <div class="col-sm-6 .d-flex">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">{{post_name}} by {{user.name}} on {{format_date created_at}}</h5>
        <p class="card-text">{{post_body}}</p>
        <a href="#" class="btn btn-primary">add comment</a>
      </div>
    </div>
  </div>